/* ==================================================
    * common types
================================================== */

export type svgIconInfo = {
    iconWidth: sizePixel,
    iconHeight: sizePixel,
    iconFill: string,
}

export type sizePixel = string | number;